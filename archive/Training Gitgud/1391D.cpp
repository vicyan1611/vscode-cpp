#include <iostream>
using namespace std;
int a[5][5];
int main()
{
    int n, m;
    if (min(n, m) > 3)
    {
        cout << -1;
        return 0;
    }
    for (int i = 1; i <= n; ++i)
    {
        for (int j = 1; j <= m; ++j)
            cin >> a[i][j];
    }
    for (int)
    return 0;
}